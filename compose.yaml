# nasvcs compose example
services:
  nasvcs:
    # image: ghcr.io/jabenninghoff/nasvcs:latest
    image: nasvcs:dev
    build:
      context: .
      args:
        - NASVCS_VERSION=0.0.1-dev
    restart: unless-stopped
    volumes:
      - ./hostkeys:/opt/nasvcs/etc/ssh
